name: kingofshoes
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter
  flutter_bootstrap: ^2.0.0
  google_maps_flutter: ^2.10.0
  geolocator: ^13.0.2
  flutter_map: ^7.0.2
  geocoding: ^3.0.0
  flutter_svg: ^2.0.16
  http: ^1.2.2
  path_provider: ^2.1.5
  image_picker: ^1.1.2
  path: ^1.8.0
  intl: ^0.20.1
  provider: ^6.1.2
  shared_preferences: ^2.3.5
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/  # Thêm dòng này để sử dụng toàn bộ thư mục hình ảnh
